name: Build iOS IPA (Sidestore)

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      
      - name: Install XcodeGen
        run: |
          # Download xcodegen from releases
          curl -sL https://github.com/yonaskolb/XcodeGen/releases/download/v0.54.1/XcodeGen.zip -o XcodeGen.zip
          unzip -o XcodeGen.zip
          ls -la

      - name: Setup project
        run: |
          cd apps/ios
          cp LocalSigning.xcconfig.example LocalSigning.xcconfig
          ../XcodeGen/bin/xcodegen generate

      - name: Build for Simulator
        run: |
          cd apps/ios
          xcodebuild -project OpenClaw.xcodeproj \
            -scheme OpenClaw \
            -configuration Debug \
            -destination 'platform=iOS Simulator,name=iPhone 16' \
            build

      - name: Build for Device (unsigned)
        run: |
          cd apps/ios
          xcodebuild -project OpenClaw.xcodeproj \
            -scheme OpenClaw \
            -configuration Release \
            -destination 'generic/platform=iOS' \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            build

      - name: Create IPA
        run: |
          APP=$(find ~/Library/Developer/Xcode/DerivedData -name "OpenClaw.app" -type d | head -1)
          mkdir -p Payload/OpenClaw.app
          cp -R "$APP" Payload/OpenClaw.app
          cd Payload
          zip -qr ../OpenClaw.unsigned.ipa *

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: OpenClaw-ipa
          path: OpenClaw.unsigned.ipa
